api = this.Jestful = (this.Jestful || {})
internal = api.internal = (api.internal || {})

<% Dir.glob(File.expand_path('../jestful/**/*.coffee', __FILE__)).each do |file| %>
### <%= file %> ###
<%= File.read(file) %>

<% end %>