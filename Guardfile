guard 'coffeescript', :output => 'spec/javascripts/compiled/src' do
  watch(/^app\/assets\/javascripts\/(.*).coffee/)
end

guard 'coffeescript', :output => 'spec/javascripts/compiled/spec' do
  watch(/^spec\/javascripts\/(.*).coffee/)
end
